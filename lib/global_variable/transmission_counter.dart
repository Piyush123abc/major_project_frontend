class FallbackCounter {
  // Global variable to maintain the QR generation index
  static int scanIndex = 1;
}

class SecureCounter {
  // Global variable to maintain the nonce generation index for the secure method
  static int scanIndex = 1;
}
